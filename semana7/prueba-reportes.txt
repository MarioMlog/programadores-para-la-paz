Al enviar un reporte mediante la ruta POST /reportes, el servidor guarda la información en un array llamado reportes. Después, al consultar la ruta GET /reportes, se muestra la lista de todos los reportes registrados mientras el servidor permanece en ejecución. Si el servidor se reinicia, la lista vuelve a quedar vacía porque los datos se almacenan únicamente en memoria.