El comando uname -a muestra información sobre el sistema operativo, el kernel y la arquitectura del equipo.

El comando whoami muestra el nombre del usuario que está utilizando el sistema.

Conocer esta información es útil para administrar un servidor porque permite identificar el sistema donde se trabaja, verificar el usuario activo y facilitar tareas de mantenimiento, configuración y seguridad.